{
  "name": "make-fetch-easier",
  "author": "veilrain",
  "license": "ISC",
  "description": "Poor man's http request utilities.",
  "version": "0.0.1",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "fetch",
    "http",
    "request"
  ],
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "form-data": "^4.0.1"
  }
}
